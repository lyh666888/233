#include<iostream>
    #include<stack>
        using namespace std;
        stack <int>s;
            int main()
            {
            cout<<s.empty()<<endl; if(s.empty()) { cout<<s.empty()<<endl; } return 0; }